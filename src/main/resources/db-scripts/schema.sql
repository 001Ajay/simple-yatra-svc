create table requester (
    requester_id        INT primary key,
    name                varchar(255),
    password            varchar(255),
    email               varchar(255),
    phone               varchar(255),
    employee_id         varchar(255),
    office         varchar(255)
);